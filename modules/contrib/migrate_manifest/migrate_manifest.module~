<?php

/**
 * @file
 * Migrate manifest module.
 *
 * Empty module file to satisfy some old assumptions in things like drush that
 * require it exist.
 */
/*function migrate_manifest_migrate_prepare_row(Row $row, MigrateSourceInterface $source, MigrationInterface $migration) {
  // Ignore content types from being migrated.
  if ($migration->id() == 'd6_node_type') {
    $ignore_content_types = array('quickinfo', 'problem', 'downtimes');
    if (!in_array($row->getSource()['type'], $ignore_content_types)) {
      return FALSE;
    }
  }

  // Ignoring content of 4 content types.
  $ignore_content_types_content = array('d6_node__quickinfo', 'd6_node__problem', 'd6_node__downtimes');
  if (!in_array($migration->id(), $ignore_content_types_content)) {
    return FALSE;
  }
}*/
