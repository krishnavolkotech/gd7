{#
/**
 * @file
 * The default template file for e-mails.
 *
 * Available variables:
 * - subject: The subject.
 * - body: The message content.
 * - message: The $message array created and used in the mail building
 *   procedure. Its content varies between cases, but typically contains at
 *   least the following elements:
 *   - id: The message identifier.
 *   - module: The module that handles the building of the message.
 *   - key: The key of the message.
 *   - to: The recipient email address.
 *   - from: The email address of the sender.
 *   - langcode: The langcode to use to compose the e-mail.
 *   - params: The message parameters.
 *
 * This template may be overriden by module and/or mail key, using any of the
 * following template names:
 * - swiftmailer.html.twig: global, used by default.
 * - swiftmailer--mymodule.html.twig: only emails sent by the module "mymodule".
 * - swiftmailer--mymodule--test.html.twig: only emails by the module
 *   "mymodule" with key "test".
 *
 * @see template_preprocess()
 * @see template_preprocess_swiftmailer()
 *
 * @ingroup themeable
 */
#}
<html>
<head>
<style type="text/css">
table tr td {
  font-family: Arial;
  font-size: 12px;
}
.quickinfo ul,
.quickinfo ol,
.quickinfo .ckeditor-custom-wrapper .field ul,
.quickinfo .ckeditor-custom-wrapper.field ol{
  float: left;
  padding-left: 0 !important;
  width: 100%;
}
.quickinfo .field--name-upload.field--type-file table tbody tr td .hzd-file-created,
.quickinfo .field--name-upload.field--type-file table tbody tr td .file-size{
  display:none;
}
.quickinfo .field--name-upload.field--type-file table tbody tr td .file-link a{
  text-decoration:none;
}
.quickinfo .field--name-upload.field--type-file table,
.quickinfo table {
  border-collapse: collapse;
  color: #000;
  font-size: 11px;
  line-height: 20px;
  margin-bottom: 10px;
  width: 100% !important;
}
.quickinfo table{
  border:1px solid #000;
}
.quickinfo .field--name-upload.field--type-file table tr.odd {
  background-color: #e9f3fb;
}
.quickinfo .field--name-upload.field--type-file table thead {
    background: #f5f5f5 none repeat scroll 0 0;
}
.quickinfo table tbody tr td {
    border: 1px solid rgb(212, 231, 245);
    border-radius: 0;
    line-height: 150%;
    padding: 5px 8px 7px;
    text-align: left;
}
h1{
  font-size:20px;
  color: #004070 !important;
  font-weight: 500 !important;
}
article.quickinfo h2 {
  width: 100%;
  float: left;
  font-size: 20px !important;
  margin: 10px 0 !important;
  color: #004077;
}
article.quickinfo .field .field--label{
  margin-bottom: 3px;
  font-weight:700;
}
.alert a, .field--label, .file{
  font-weight:700;
}
article.downtimes .form-group, 
article.quickinfo .field--name-field-unique-id, 
article.quickinfo .field, article.quickinfo .field .field--label,
article.quickinfo .field{
   width:100%;
   float:left;
 }
article .quickedit-field, article.quickinfo .field{
  margin-bottom:15px;
}
article.quickinfo .field .field--item, 
article.quickinfo .field .field--items {
  width: 98%;
  display:block;
  margin: 0 0 3px 0%;
  padding: 0;
}
.quickinfo .field--name-upload.field--type-file table thead tr th {
  border-left: 1px solid rgb(212,231,245);
  background: #d4eaf5;
  text-align: left;
  padding-right: 1em;
  height: 45px;
  font-family: arial,sans-serif;
  text-indent: 7px;
  border-radius: 0 !important;
}
.quickinfo .field--name-upload.field--type-file table tbody tr.odd td {
  background-color: #e9f3fb;
}
.quickinfo .field--name-upload.field--type-file table tbody tr.even td {
  background-color: #fff;
}
.quickinfo .field--name-upload.field--type-file table,
.quickinfo .field--name-upload.field--type-file table tbody tr{
  border:0;
}
table.header-table tr td{
  padding:10px;
}
</style>

</head>
<body>
<div>
  <table width="800px" border="0" cellpadding="0" cellspacing="0" class="header-table" style="margin-bottom:20px;">
    <tr>
      <td>
        <div style="padding: 0px 0px 0px 0px;">
          {{ body }}
        </div>
      </td>
    </tr>
  </table>
</div>
</body>
</html>
